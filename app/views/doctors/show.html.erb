<h1>Dr. <%= @doctor.name %></h1>
<h2>Department: <%= @doctor.department %></h2>
<h2>Appointments:</h2>
<ul>
  <%= @doctor.appointments.each do |app| %>
    <li><%= link_to app.patient.name, patient_path(app.patient) %> - <%= app.time %></li><br>
    <% end %>
</ul>
